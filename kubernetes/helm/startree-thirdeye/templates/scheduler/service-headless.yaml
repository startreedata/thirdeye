
{{ if .Values.scheduler.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ template "thirdeye.scheduler.headless" . }}
  labels:
    app: {{ include "thirdeye.name" . }}
    chart: {{ include "thirdeye.chart" . }}
    component: {{ .Values.scheduler.name }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  clusterIP: None
  ports:
    # [pod_name].[service_name].[namespace].svc.cluster.local
    - name: admin
      protocol: TCP
      port: 8081
      targetPort: 8081
    {{- if .Values.tls.enabled }}
    - name: https
      protocol: TCP
      port: {{ .Values.scheduler.tlsPort }}
      targetPort: {{ .Values.scheduler.tlsPort }}
    {{- else }}
    - name: http
      protocol: TCP
      port: {{ .Values.scheduler.port }}
      targetPort: {{ .Values.scheduler.port }}
    {{- end }}
  selector:
    app: {{ include "thirdeye.name" . }}
    release: {{ .Release.Name }}
    component: {{ .Values.scheduler.name }}
{{ end }}
