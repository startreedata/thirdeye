{{- if and .Values.traefik.enabled ( not .Values.tls.enabled ) -}}
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: {{ printf "%s-%s" (include "thirdeye.traefik.fullname" . ) "thirdeye" | trunc -63 }}
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`{{ .Release.Name }}.{{ .Release.Namespace }}.{{ required "domain is required." .Values.domain }}`)
      kind: Rule
      services:
        - name: {{ include "thirdeye.ui.headless" . }}
          namespace: {{ .Release.Namespace }}
          port: {{ .Values.ui.port }}
{{- end }}