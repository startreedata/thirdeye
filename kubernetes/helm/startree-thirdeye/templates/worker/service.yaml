
{{ if .Values.worker.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "thirdeye.worker.fullname" . }}
  labels:
    app: {{ include "thirdeye.name" . }}
    chart: {{ include "thirdeye.chart" . }}
    component: {{ .Values.worker.name }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.worker.serviceType }}
  ports:
    # [pod_name].[service_name].[namespace].svc.cluster.local
    - name: admin
      protocol: TCP
      port: 8081
      targetPort: 8081
    {{- if .Values.tls.enabled }}
    - name: https
      protocol: TCP
      port: {{ .Values.worker.tlsPort }}
      targetPort: {{ .Values.worker.tlsPort }}
    {{- else }}
    - name: http
      protocol: TCP
      port: {{ .Values.worker.port }}
      targetPort: {{ .Values.worker.port }}
    {{- end }}
  selector:
    app: {{ include "thirdeye.name" . }}
    release: {{ .Release.Name }}
    component: {{ .Values.worker.name }}
{{ end }}
