import { makeStyles } from "@material-ui/core";

export const useErrorIndicatorStyles = makeStyles({
    errorIndicator: {
        height: "100%",
    },
    icon: {
        lineHeight: 1,
    },
});
