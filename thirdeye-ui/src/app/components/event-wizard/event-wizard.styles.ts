import { makeStyles } from "@material-ui/core";

export const useEventWizardStyles = makeStyles({
    controlsContainer: {
        flexGrow: 1,
    },

    pageContents: {
        width: "100%",
    },
});
