import { makeStyles } from "@material-ui/core";

export const useMetricsWizardStyles = makeStyles({
    controlsContainer: {
        flexGrow: 1,
    },
    fieldContainer: {
        height: "80px",
    },
    selectContainer: {
        width: "100%",
        paddingLeft: "10px",
        marginTop: "-24px",
    },
});
