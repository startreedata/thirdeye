import { makeStyles } from "@material-ui/core";

export const useNoDataIndicatorStyles = makeStyles(() => ({
    noDataIndicator: {
        height: "100%",
        width: "100%",
        textAlign: "center",
    },
}));
