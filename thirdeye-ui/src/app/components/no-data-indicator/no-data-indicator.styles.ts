import { makeStyles } from "@material-ui/core";

export const useNoDataIndicatorStyles = makeStyles({
    noDataIndicator: {
        height: "100%",
    },
    icon: {
        lineHeight: "1px",
    },
});
