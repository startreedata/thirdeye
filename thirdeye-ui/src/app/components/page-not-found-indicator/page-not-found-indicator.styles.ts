import { makeStyles } from "@material-ui/core";

export const usePageNotFoundIndicatorStyles = makeStyles({
    errorCode: {
        fontSize: "80px",
        fontWeight: "normal",
        lineHeight: "89px",
    },
});
