import { makeStyles } from "@material-ui/core";

export const useTransferListStyles = makeStyles({
    list: {
        height: "250px",
        overflowX: "hidden",
        overflowY: "auto",
        padding: "0px",
    },
});
