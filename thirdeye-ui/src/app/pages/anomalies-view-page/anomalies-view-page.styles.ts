import { makeStyles } from "@material-ui/core";

export const useAnomaliesViewPageStyles = makeStyles(() => ({
    fullHeight: {
        height: "100%",
    },
    linkButton: {
        display: "flex",
        alignItems: "center",
    },
}));
