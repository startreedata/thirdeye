import { ReactNode } from "react";

export interface InfoIconV1Props {
    autoFitToContents?: boolean;
    displayInline?: boolean;
    enablePadding?: boolean;
    className?: string;
    children?: ReactNode;
}
