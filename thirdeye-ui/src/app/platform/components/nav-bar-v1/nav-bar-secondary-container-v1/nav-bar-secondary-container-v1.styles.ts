import { makeStyles } from "@material-ui/core";

export const useNavBarSecondaryContainerV1Styles = makeStyles({
    navBarSecondaryContainer: {
        display: "flex",
        flex: 1,
        flexDirection: "column",
        justifyContent: "flex-end",
    },
});
