import { makeStyles } from "@material-ui/core";

export const usePageV1Styles = makeStyles({
    page: {
        display: "flex",
        flex: 1,
        flexDirection: "column",
        overflow: "auto",
    },
});
