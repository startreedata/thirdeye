import { typographyOptions } from "./typography-util";

// CortexData Material-UI theme style overrides for Tooltip
export const tooltipClasses = {
    tooltip: {
        ...typographyOptions.body2,
    },
};
