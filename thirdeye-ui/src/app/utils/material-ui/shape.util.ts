import { ShapeOptions } from "@material-ui/core/styles/shape";
import { Dimension } from "./dimension.util";

// Material-UI theme shapes
export const shapeOptions: ShapeOptions = {
    borderRadius: Dimension.RADIUS_BORDER_DEFAULT,
};
